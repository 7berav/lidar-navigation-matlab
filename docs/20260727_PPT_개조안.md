# 연구미팅 PPT 개조안 (D 챕터 중심)

작성일: 2026-07-27 · 대상 `docs/연구미팅_7월_피팅.pptx` (현재 23장) · 브랜치 `paper/2026taes`

> 용도: 슬라이드별 **텍스트 수정 지시서**. 그림은 아직 삽입하지 않고 **참조 폴더/파일 경로만** 명시.
> 원칙 — §3.1 = 완료(계획 미세수정) / §3.2 = 진행중(계획 유지 + W 최종수정·회수테스트 합본 예고) / §3.3 = 미착수(확장 설계).

---

## 0. 전체 방침

- **D 챕터 = [설계 1장] + [결과 N장] 구조로 재편.** 현재는 설계(Expected)만 있고 결과 페이지가 없음.
- **§3.1**: 지표 프레임을 실제 진행분에 맞춰 갱신 — `df=tr(H_λ)` 좌표 도입, **CD 3분할(visible/hidden/all)**, **HD95** 반영.
- **§3.2**: 계획 그대로 유지. **ω(W) update ON/OFF 살림.** 단 "현재 진행중 / 최종 W수정 후 회수테스트 합본 그래프" 라인 추가.
- **§3.3**: 결과 없음 → 뒷부분 강화용 3단계 확장 설계로 채움.
- **섹션 divider 정리**: 현재 A·B·D·E·F만 존재, **C 없음**(슬라이드 5~8 이론부가 divider 없이 떠 있음). E(슬라이드 21)는 설계 슬라이드들 *뒤에* 위치 → 순서 오류.

---

## 1. 슬라이드별 개조 지시 (현재 번호 기준)

### S1 — 표지
- `2026 4월 연구미팅` → **`2026 7월 연구미팅`**
- `April, 2026` → **`July, 2026`**

### S2 — Outline
- 현재: Introduction / Problem Formulation / Spectral-Clustering HERO Algorithm / Simulation & Result / Conclusion
- 수정: **`Simulation & Result`을 하위 3항목으로 전개**
  - `▹ §3.1 Basis & Regularization (done)`
  - `▹ §3.2 Convergence & Robustness (in progress)`
  - `▹ §3.3 ISS Multi-view (planned)`
- `Conclusion` → **`Conclusion & Future Works`**

### S3 (A) — Introduction / S4 (B) — Problem Formulation
- **수정 없음** (divider)

### S5~S8 — 이론부 (Homogeneous poly / Fischer / Ridge / Sobolev Ridge)
- 내용 유지. 다만 아래 2건만:
- **S8 (Sobolev Ridge)** 하단에 1줄 추가 — 3.1과의 연결고리
  - `▹ Penalty strength enters only through effective dof: df(λ) = tr(H_λ)`
- **[선택] C divider 신설** — S5 앞에 `C  Methodology (HERO)` 한 장 추가하면 A·B·C·D·E·F 일관.
- 오타: S8 제목 `Sovolev` → **`Sobolev`**

### S9 (D) — Simulation & Result
- **수정 없음** (divider)

### S10 — Experimental Design — Overview  ★수정
- 현재 `Explicit axis: occlusion ratio {0, 10, 20, 40%}`
  → **`Explicit axis: occlusion ratio {10, 20, …, 70%} (§3.1 diagnostic), fixed 30% for §3.2`**
- `Three sections` 하위에 **진행상태 태그** 부기
  - `§3.1 … — what to use, fix λ` → 뒤에 **`[done]`**
  - `§3.2 … — how many RANSAC iterations` → 뒤에 **`[in progress]`**
  - `§3.3 … — does it work in practice` → 뒤에 **`[planned]`**
- **`Metric suite` 블록 신설** (Shared design principles 아래)
  - `▹ Stability: κ(ΦᵀΦ/N), ‖β‖²`
  - `▹ Surface: Chamfer split into visible / hidden / all, + HD95`
  - `▹ Detection: precision / recall / F1`
  - `▹ Cost: N_iter, wall time`
- **`Common coordinate` 1줄 추가** — 이 덱의 핵심 프레임
  - `▹ df(λ)=tr(H_λ) as the comparison coordinate — comparable across sample size and degree`
- 상수 1줄 추가: `▹ Setup: N=4000 pts, σ_noise=0.007, τ=0.45, conf=0.95`

### S11 — Experimental Design — §3.1 Basis & λ  ★수정 (완료분 반영)
- 제목: `Experimental Design — §3.1 Basis & λ` → **`§3.1 Basis, Regularization & Effective dof  [done]`**
- **`Fair comparison setup` 블록**: Monomial vs Fischer κ 비교는 **실측 산출물 없음** → 둘 중 택1
  - (a) **권장** — 블록을 **`Design axes`** 로 교체:
    - `▹ λ grid: OLS(3e-5) → 3e0, log-spaced`
    - `▹ Sample multiplier k = {1.0, 1.2, 1.4} (minimal-sample multiple)`
    - `▹ Occlusion {10 … 70%}, 10 views per cell`
    - `▹ Degree {4, 6}  (p = 15 / 28)`
    - `▹ Shapes: hexagon, cube, cylinder, sphere, ellipsoid`
  - (b) 유지하려면 → Monomial baseline 실행이 **TODO**임을 명기
- **`Metrics` 블록 교체** (현재: κ, ‖β‖², Chamfer(vis), Chamfer(occ), time)
  - `▹ df(λ) = tr(H_λ) — effective degrees of freedom (primary coordinate)`
  - `▹ Chamfer — split into visible / hidden / all (3-way)`
  - `▹ HD95 — 95th-percentile Hausdorff (tail-sensitive)`
  - `▹ ‖β‖² — coefficient norm (ridge suppression)`
  - `▹ df* gap = df*_subset − df*_full — size-invariance diagnostic`
- **`Questions` 블록 신설**
  - `▹ Q1. Is λ transferable across sample size? → use df, not raw λ`
  - `▹ Q2. Does the optimal df survive occlusion? → df* gap vs occ`
  - `▹ Q3. Is degree 6 necessary for pose? → 4th vs 6th at matched df`

### S12 — Experimental Design — §3.1 Expected Results  ★대폭 수정
- 제목: `— §3.1 Expected Results` → **`§3.1 Findings`** (Expected → 확정 결과)
- `λ selection` 블록 → **`Key findings`** 로 교체
  - `▹ F1. df(λ) decreases monotonically; df → min(n, p) as λ → 0`
  - `▹ F2. Size-invariance holds only at low occlusion — df* gap ≈ −0.7 (occ10) → −5.9 (occ50) → −14.2 (occ70), hexagon`
  - `▹ F3. Optimal df splits by region — visible df≈20, all-surface df≈15, hidden df≈6 (order 6, occ30)`
  - `▹ F4. Degree is secondary to df — 4th CD 0.197@df 7.9 ≈ 6th CD 0.210@df 15`
  - `▹ F5. Ridge benefit is 2–3× larger on the hidden region than on the full surface`
- `Expected figures` 블록 (§3.1a~d) → **삭제**, 아래 §2의 결과 슬라이드로 이관
- `§3.1 verdict` 블록 → **`Decisions carried into §3.2`** 로 교체
  - `▹ Degree: 4th for hypothesis / pose, 6th for final refit (coarse-to-fine)`
  - `▹ λ fixed (no runtime df adaptation) — per-draw df CV < 9% at fixed n`
  - `▹ df is a diagnostic, not a runtime parameter`
- 마지막 인용줄 `"All subsequent experiments use Fischer + Sobolev, λ = λ*"` → **유지하되** `λ = λ*` 뒤에 `(re-identified end-to-end in §3.2)` 부기

### S13 — Experimental Design — §3.2 Convergence  ★계획 유지 + 보강
- 제목 뒤에 **`[in progress]`** 부기
- **`ω-update ON vs OFF` 라인 — 유지** (사용자 요청). 아래를 *추가*만:
  - `▹ λ ablation: OLS → 3e0 (end-to-end optimum may differ from §3.1)`
  - `▹ Outlier ratio ε = {0, 10, 20, 30, 40%}`
  - `▹ Outlier type: uniform vs structured (two-object)`
  - `▹ Iteration budget: maxIter 1000 vs 3000 (saturation check)`
- **`Expected result` 블록에 추가**
  - `▹ Final: W (ω-weighting) refined at the end → combined plot with recovery test`
  - 기존 `ω-update ON converges faster…` 유지
- 하단 상태 1줄
  - `▹ Status: λ ablation & ε sweep done at baseLam=1e-1 (preliminary); rerun at λ* pending`

### S14 — Experimental Design — §3.3 ISS Simulation  ★확장 (뒷부분 강화)
- 제목 뒤에 **`[planned]`** 부기
- 기존 §3.3.1 / §3.3.2 유지 + **중간 단계 신설** (합성→실측 간극 메우기)
  - `▸ Step 1 — Occlusion robustness (synthetic)`
    - `▹ Open occlusion axis {10 … 70%}; headline: CD_hidden vs occ, λ=0 vs λ* overlay`
    - `▹ Add Volumetric IoU — implicit f=1 is a closed volume; grid inside/outside is free`
  - `▸ Step 2 — §3.3.1 Single real object (CubeSat / KOMPSAT)` ← 기존 내용 흡수
  - `▸ Step 3 — §3.3.2 Multi-body (ISS) spectral clustering` ← 기존 내용 유지

### S15 — Contributions and Current Status  ★수정
- `Now: §3.1 — running λ grid Monte Carlo, collecting Chamfer curves`
  → **`Done: §3.1 — df-based diagnosis; degree 4 + fixed λ decided`**
- `Next: fix λ* → §3.2 convergence → §3.3 ISS simulation`
  → **`Now: §3.2 — RANSAC convergence & outlier robustness (ω-update, λ*)`**
  → **`Next: §3.3 — occlusion axis → real scan → ISS spectral clustering`**
- C2 문구 `Fischer basis → structural κ reduction + Sobolev Ridge justification`
  → κ 실측이 아직 없으므로 **`… + effective-dof based λ selection`** 로 완화 권장

### S16~S20 — ridge λ 스윕 / 표본부족 이미지 (비공식)  ★재배치
- **S16·S17** (λ 0.1/0.01/none, 10/1/0.3) → **§3.1 결과 R6 한 장으로 통합** (아래 §2 참조). 나머지는 백업(Appendix)으로.
- **S18** (Final model with different penalty) → **R6에 흡수**
- **S19·S20** (under/over-determined, λ 0.01 / 0.3) → **§3.1 결과 R2 옆 보조**로 이동. 제목을
  `HERO modification - Ridge` → **`§3.1 — Sample size vs regularization`** 로 변경
- 공통: 이 5장의 제목이 전부 `HERO modification - Ridge`로 중복 → **결과 슬라이드 제목 규칙**(`§3.1 R#. <내용>`)으로 통일

### S21 (E) — Experimental Design  ★순서 오류
- 설계 슬라이드(S10~S14) **뒤에** 있음 → 둘 중 택1
  - (a) **권장** — 이 divider를 **S9와 S10 사이로 이동**하거나 삭제(D 안에 포함되므로 중복)
  - (b) 유지하려면 제목을 **`E  Discussion`** 등으로 변경

### S22 (F) — Future Works
- divider 유지. 뒤에 **내용 슬라이드 1장 신설** (현재 F 뒤가 바로 Plan 2뿐이라 빈약)
  - `▸ Near-term`
    - `▹ Rerun §3.2 sweeps at λ* (ε, shape, two-object axes)`
    - `▹ Two-stage order solver: hypothesis 4th / local refit 6th`
    - `▹ Scoring ablation: τ multiplier, MSAC, IRLS weighting`
  - `▸ Mid-term`
    - `▹ Volumetric IoU as primary occlusion metric`
    - `▹ Real LiDAR scans (DATA/*.ply)`
  - `▸ Long-term`
    - `▹ Spectral clustering → GNN / attention (Plan 2)`
    - `▹ Control integration (CBF / MPC) — Paper 2`

### S23 — Plan 2
- 유지. 위 Future Works 슬라이드의 `Long-term`과 중복되므로 **한쪽으로 통합** 권장.

---

## 2. 신규 결과 슬라이드 명세 (그림 미삽입 · 경로만)

> 슬라이드 제목 규칙: **`§3.x R#. <한 줄 메시지>`**
> 각 항목 = [참조 폴더/파일] · [무엇을 비교] · [리포트 1줄]

### §3.1 결과 (S12 뒤에 삽입)

**R1. λ alone is not comparable across sample sizes**
- 파일: `test_ridge/20260723_1312_hexagon_or6/fig1_lcurve.png`
- 비교: L-curve(잔차 MSE vs βᵀWβ), 표본배수 k={1.0,1.2,1.4} × occ={30,50,70%}
- 리포트: *elbow가 k·occ마다 3e-2~1e0로 흩어짐 → λ를 좌표로 쓰면 비교 불가.*

**R2. df(λ) as the common coordinate**
- 파일: `test_ridge/20260723_1312_hexagon_or6/fig5_df.png`
- 보조: 같은 폴더 `fig5b_df_all_k.png` / 표본크기 시각화는 S19·S20 이미지
- 비교: df=tr(H_λ) vs λ, 소표본(k=1.4) vs 전체가시(점선), occ 3패널
- 리포트: *df는 λ→0에서 min(n,p)로 수렴하며 단조감소; occ↑일수록 소표본·전체점 df 괴리 확대.*

**R3. Occluded-region error and HD95 vs λ**
- 파일: `test_ridge/20260723_1312_hexagon_or6/fig2_cd_hd_occ.png`
- 보조(전체표면): 같은 폴더 `fig2_cd_hd_all.png`
- 비교: CD_occ·HD95 vs λ, k 3종 + all-visible, occ={30,50,70%}
- 리포트: *숨은영역 오차가 λ에 대해 U자; 최적 λ가 occlusion에 따라 이동.*

**R4. Size-invariance breaks above 50% occlusion**
- 파일: `test_ridge/aggregate_20260722_1036/aggA_df_gap_vs_occ.png`
- 비교: df\* 괴리(subset−full) vs occ, 도형 5종(hexagon/cube/cylinder/sphere/ellipsoid)
- 리포트: *occ≤40%에선 괴리가 좁고 ≥50%에서 급확대(cube +21) → 붕괴점이 강건성 진단량.*

**R5. Optimal df is pulled down by the full-surface Chamfer  ★§3.1 대표**
- 파일: `test_ridge/aggregate_20260722_1036/aggB_cd_vs_df.png`
- 비교: CD_all vs df, 실선=소표본 / 점선=전체가시 / ★=최적, occ={30,50,70%}
- 리포트: *완전표면 CD가 df\*를 블롭(df≈8) 쪽으로 편향 → λ_hyp(보이는 적합) ≠ λ_refit(복원).*

**R6. What over/under-regularization looks like**
- 파일: `test_ridge/20260723_1312_hexagon_or6/fig3_o30_k14.png`
- 대안(가림 강도별): 같은 폴더 `fig3_o50_k14.png`, `fig3_o70_k14.png` / 표본배수별 `fig3_o30_k10.png`, `fig3_o30_k12.png`
- 비교: 3D 복원 등위면 OLS / λ=1e-3 / λ=1e0, 파랑=가시점·회색=숨은 GT
- 리포트: *OLS는 터널·혹이 사방으로 폭발, 과정규화는 구(球)로 뭉갬 — 그 사이에 최적이 존재.*

**R7. Ridge helps most where you cannot see**
- 파일: `test_ridge/20260723_1312_hexagon_or6/fig4_benefit.png`
- 비교: 릿지 이득 CD(OLS)−min_λ CD, 숨은영역 vs 전체표면, occ 10~70%
- 리포트: *숨은영역 이득이 전체 대비 2~3배 → 정규화의 값어치는 가림 복원에서 나온다.*

**R8. Degree 4 vs 6 at matched df**  *(그림 미생성 — §4 참조)*
- 참조 데이터: `test_ridge/20260723_1312_hexagon_or4/` ↔ `20260723_1312_hexagon_or6/`
- 도형 확장: `20260723_1322_{cube,cylinder}_or{4,6}/`
- 리포트: *같은 df로 맞추면 4차 CD 0.197@df7.9 ≈ 6차 0.210@df15 → 차수보다 df가 지배.*

### §3.2 결과 (S13 뒤에 삽입 · **전부 preliminary 표기**)

**R9. End-to-end optimal λ = 1e-2  ★§3.2 대표**
- 파일: `test_ridge/sec32_diag_20260723_1802/perf_vs_lambda.png`
- 비교: F1·CD vs λ (hexagon, ε=0.2), OLS~1e0
- 리포트: *λ\*=1e-2에서 F1 0.960 / CD 0.064 내부최적; §3.1 소표본 최적(1e-1)보다 한 자릿수 아래 = refit regime.*

**R10. Why OLS explodes (visual)**
- 파일: `test_ridge/sec32_diag_20260723_1802/shape_single_omega.png`
- 보조(잔차색): 같은 폴더 `shape_single_resid.png`
- 비교: 복원 등위면 + 점별 ω, OLS / 1e-2 / 1e-1
- 리포트: *OLS 등위면이 안장으로 뒤틀려 박스를 벗어남 — ‖β‖² 폭발의 시각적 정체.*

**R11. …and the coefficient norm confirms it**  *(R10과 세트 배치)*
- 파일: `test_ridge/sec32_diag_20260723_1802/iterts_single_scores.png`
- 비교: iter별 preScore/postScore/w/inlierR/‖β‖²(log)/mseInlier
- 리포트: *OLS ‖β‖² 10²~10⁴ vs 릿지 ~10⁰ — 3D 폭발이 계수 노름 폭발과 일치.*

**R12. RANSAC convergence**
- 파일: `test_ridge/sec32_diag_20260723_1802/iterts_single_prf.png`
- 비교: iter별 P/R/F1 산점 + running-best F1 실선, OLS/1e-2/1e-1
- 리포트: *running-best F1이 계단 상승 후 포화 = 수렴; ridge 계단이 OLS 위.*

**R13. Iteration budget saturates at 1000**
- 파일: `test_ridge/sec32_single_20260723_1701/fig2_convergence.png`
- 비교: maxIter 1000 vs 3000
- 리포트: *F1 0.857 vs 0.859로 동일 → N_iter=1000 고정, §3.3에 이식.*

**R14. Robustness to uniform outliers**
- 파일: `test_ridge/sec32_single_20260723_1701/fig1_F1_vs_eps.png`
- 보조(기하오차): 같은 폴더 `fig3_cd_hd_vs_eps.png`
- 비교: F1 vs ε(0~40%), λ 6종 + 도형 3종, 굵은선=3.1 앵커밴드
- 리포트: *ε 0→40%에서 F1 0.905→0.79 완만 저하, precision 주도 하락·recall 안정.* **(λ=1e-1 예비본)**

**R15. Structured outliers are harder**
- 파일: `test_ridge/sec32_compare_20260723_1523/fig4_uniform_vs_structured.png`
- 비교: 균일 vs 구조적(2물체), F1·CD vs ε
- 리포트: *ε=0.4에서 CD 0.22(균일) vs 0.45(구조적) — 방해물이 국소적으로 인라이어처럼 보임.* **(λ=1e-1 예비본)**

**R16. Two-object scene: where ω goes**
- 파일: `test_ridge/sec32_diag_20260723_1802/shape_twoobj_omega.png`
- 보조: 같은 폴더 `iterts_twoobj_prf.png`, `iterts_twoobj_scores.png` / 오프셋·스킴 축은 `test_ridge/sec32_twoobj_20260723_1514/fig1_twoobj_F1.png`, `fig2_twoobj_cd.png`
- 비교: 타깃 A + 방해물 B, ω 분포 + 복원면, OLS/1e-2/1e-1
- 리포트: *ω가 타깃에 집중하고 방해물은 회색으로 물러남 — 오프셋이 커지면 F1은 불변이나 CD 급증(0.24→0.61).*

**R17. 4th order suffices for pose**
- 파일: `test_ridge/sec32_single_20260723_1701/fig5_order4_vs_6.png`
- 비교: 4차 RANSAC vs 6차 post-hoc refit, CD·HD95
- 리포트: *6차 refit이 완전표면 CD에서 무이득(6차>4차) → 자세추정은 4차로 충분, 표본효율까지 유리.*

**R18. ω-update ON vs OFF + recovery test**  *(미실행 — 계획)*
- 상태: **§3.2 진행중 항목.** 최종 W 수정 후 회수(recovery) 테스트까지 **합본 그래프 1장**으로 작성 예정.
- 산출 예정 위치: `test_ridge/sec32_*/` (신규 폴더)

### §3.3 결과
- **현재 없음.** S14 확장 설계(Step 1~3)에 따라 생성 예정.

---

## 3. 개조 후 슬라이드 순서 (제안)

| # | 내용 | 상태 |
|---|---|---|
| 1–2 | 표지 / Outline | 수정 |
| 3 (A) | Introduction | 유지 |
| 4 (B) | Problem Formulation | 유지 |
| 5 (C) | *Methodology divider* | **신설(선택)** |
| 6–9 | Poly / Fischer / Ridge / Sobolev | 유지(+1줄) |
| 10 (D) | Simulation & Result | 유지 |
| 11 | Design Overview | 수정 |
| 12 | §3.1 설계 | 수정 |
| 13 | §3.1 Findings | 대폭수정 |
| 14–20 | **§3.1 결과 R1–R8** | **신설** |
| 21 | §3.2 설계 | 계획유지+보강 |
| 22–30 | **§3.2 결과 R9–R18** | **신설** |
| 31 | §3.3 설계 (Step 1–3) | 확장 |
| 32 | Contributions & Status | 수정 |
| 33 (F) | Future Works divider | 유지 |
| 34 | Future Works 내용 | **신설** |
| — | Appendix: 기존 S16–S20 잔여 | 이동 |

---

## 4. 미생성 그림 (만들면 좋음)

1. **4차 vs 6차 same-df 오버레이** (R8용) — 현재 `aggB_cd_vs_df.png`에 도형별로 섞여 있어 차수 비교가 안 보임. hexagon occ30에서 or4/or6 CD-vs-df 곡선 2개만 겹친 그림 필요.
   - 소스: `test_ridge/20260723_1312_hexagon_or{4,6}/lcurve_demo_results.csv`
2. **표본효율 막대/표** — 4차 minimal sample 21점 vs 6차 39점, w=0.6~0.8에서 wⁿ 비 **56~614배**. §3.1 F4·§3.2 R17의 결정적 근거인데 현재 말로만 존재.
3. **CD_hidden(또는 IoU) vs occ — λ=0 vs λ\* 오버레이** (§3.3 Step 1 헤드라인) — "정규화가 고차 곡면의 occlusion 민감성을 완화"를 정면 입증. 기존 3.1 데이터 재조합으로 가능.

---

## 5. 발표 시 주의

- **§3.2 결과는 모두 `baseLam=1e-1`(suboptimal) 예비본.** λ\*=1e-2는 hexagon/ε=0.2 한 점에서만 검증됨 → R14·R15·R16 슬라이드에 **`preliminary — rerun at λ* pending`** 각주 필수.
- **§3.1의 Monomial vs Fischer κ 비교는 실측 없음.** S11에서 이 프레이밍을 유지하려면 별도 실행 필요(현재는 df 진단 프레임으로 교체 권장).
- occlusion 축 표기가 덱({0,10,20,40%})과 실제(3.1: 10~70%, 3.2: 30% 고정)로 불일치 → S10에서 통일.

---

## 6. 참고

- `docs/20260723_3.1-3.2_진행정리.md` — 수치·결정 원본
- `docs/20260721_릿지-RANSAC-3.1-3.2-재구조화.md` — df 좌표계 이론
- `docs/lambda_ransac_section_design.md` — λ_hyp/λ_refit 분리
- `docs/occlusion_lambda_evaluation_design.md` — 지표(CD/HD/F-score/IoU), occlusion 프로토콜
