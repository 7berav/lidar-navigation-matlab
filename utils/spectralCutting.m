function labels = spectralCutting(P, k, varargin)
%SPECTRALCUTTING  Spectral clustering / cut of a point cloud.
%   labels = spectralCutting(P, k, ...) partitions point cloud P (N-by-3)
%   into k clusters via spectral cutting, returning an N-by-1 label vector.
%
%   STUB: implementation pending.

    error('spectralCutting:notImplemented', 'spectralCutting is not yet implemented.');
end
